{
  "name": "decmpfs",
  "version": "0.1.0",
  "description": "Write files straight to OS transparent filesystem compression (APFS decmpfs / btrfs / NTFS) in one pass.",
  "license": "MIT",
  "repository": "https://github.com/jdalton/decmpfs",
  "keywords": ["apfs", "btrfs", "compression", "decmpfs", "ntfs"],
  "main": "index.js",
  "files": ["index.js", "*.node"],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true,
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "cargo build --release && cp target/release/libdecmpfs_node.dylib decmpfs.node",
    "test": "node --test"
  }
}
